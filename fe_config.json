{
  // forever config for NS Departures slack bot
  "uid": "app",
  "outFile": "../log/all.log",
  "append": true, // append the logs, do not overwrite
  "script": "dist/index.js"  // main startup script (almost like issuing node server.js)
}
